"""Service-level admin operations.

Reserved for ``AuthAdmin`` async functions extracted from the
blunder_tutor CLI in TREK-58 (P4.3). Currently empty: the CLI body
still lives in ``blunder_tutor/cli/auth.py`` and reaches in directly.
"""
